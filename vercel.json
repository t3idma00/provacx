{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "cd packages/database && PRISMA_CLI_BINARY_TARGETS=rhel-openssl-3.0.x pnpm prisma generate && cd ../.. && pnpm --filter @provacx/web build",
  "installCommand": "corepack enable && pnpm install --no-frozen-lockfile",
  "outputDirectory": "apps/web/.next"
}
